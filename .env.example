# Optional: Alpha Vantage key for compact daily OHLCV (browser request).
# https://www.alphavantage.co/support/#api-key
VITE_ALPHA_VANTAGE_API_KEY=

# Server-only OpenAI key for /api/ai route.
# Do NOT prefix with VITE_ (this must stay private).
OPENAI_API_KEY=
